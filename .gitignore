# ── Logs ────────────────────────────────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# ── Runtime data ───────────────────────────────────────────────────────
pids
*.pid
*.seed
*.pid.lock

# ── Instrumented libs ─────────────────────────────────────────────────
lib-cov

# ── Coverage / tests ──────────────────────────────────────────────────
coverage
*.lcov
.nyc_output

# ── Grunt ─────────────────────────────────────────────────────────────
.grunt

# ── Bower ─────────────────────────────────────────────────────────────
bower_components

# ── node-waf ─────────────────────────────────────────────────────────
.lock-wscript

# ── Compiled addons ───────────────────────────────────────────────────
build/Release

# ── Dependencias ──────────────────────────────────────────────────────
node_modules/
jspm_packages/
web_modules/          # Snowpack

# ── TypeScript ────────────────────────────────────────────────────────
*.tsbuildinfo

# ── Caches opcionales ─────────────────────────────────────────────────
.npm
.eslintcache
.stylelintcache

# ── REPL history ──────────────────────────────────────────────────────
.node_repl_history

# ── Salida de `npm pack` ──────────────────────────────────────────────
*.tgz

# ── Yarn ──────────────────────────────────────────────────────────────
.yarn-integrity
# yarn v3 / PnP
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ── Variables de entorno ──────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Bundlers / caches ─────────────────────────────────────────────────
.cache
.parcel-cache

# ── Frameworks (build output) ─────────────────────────────────────────
.next
out
.nuxt
dist
.svelte-kit/
.docusaurus
.vuepress/dist
.temp
**/.vitepress/dist
**/.vitepress/cache
# Gatsby
.cache/

# ── Serverless / plataformas ──────────────────────────────────────────
.serverless/
.vercel/
.vercel_build_output/

# ── Otros servicios ───────────────────────────────────────────────────
.fusebox/
.dynamodb/
.firebase/
.tern-port
.vscode-test

# ── Turborepo / tmp ───────────────────────────────────────────────────
.turbo/
tmp/
.tmp/

# ── Vite logs ─────────────────────────────────────────────────────────
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# ── SO / editores ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
